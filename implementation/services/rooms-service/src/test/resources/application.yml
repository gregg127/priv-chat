spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: false
entry-auth-service:
  url: http://localhost:19999  # Non-existent, will be mocked
jwt:
  expiry-seconds: 900
management:
  endpoints:
    web:
      exposure:
        include: health
